const fs = require('fs');
const config = require("../config.json");

module.exports = {
    name: 'list',
    description: 'Получить список музыки',
    execute(DISCORDMessage, client) {
        
    },
};